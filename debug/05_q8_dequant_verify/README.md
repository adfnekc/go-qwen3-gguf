# 调查 05：Q8_0 反量化结果正确性验证

## 目的

使用 Python 的 `gguf` 库独立读取模型的 Q8_0 量化权重，并与 Go 实现的反量化结果对比。
验证 Go 的 `DequantizeQ8_0` 函数是否正确，排除权重加载错误导致模型输出错误的可能性。

## 背景

前序调查已确认：
- 模型内部自洽（cache 与 one-shot 结果一致，max diff = 0）
- KV cache 工作正常
- 权重统计值看起来合理

但模型输出仍然错误，需要独立验证反量化结果是否正确。

## 测试

| 文件 | 描述 |
|------|------|
| `check_dequant.py` | 用 Python 读取 Q8_0 tensor 并反量化，与 Go 结果对比 |
