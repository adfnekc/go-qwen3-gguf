# 调查 03：权重加载验证

## 目的

`model.Forward()`（使用 MatMulTransposed）和内联调试程序（使用旧的 MatMul）
都产生错误输出。它们的共同因素是权重加载。本调查验证权重是否从 GGUF 
文件中正确反量化和加载。

## 假设

如果权重加载出现字节偏移错误或步长错误，所有下游计算无论使用哪种 MatMul 
变体都会产生错误结果。

## 测试

| 文件 | 描述 |
|------|------|
| `check_embed.go` | 验证 token 嵌入是否合理，打印前几个值并检查 RMS |
